# Managed by Ansible. Local changes will be overwritten.

# Hostname and domain-name changes
{% for audit_architecture in _audit_rule_architectures %}
-a always,exit -F arch={{ audit_architecture }} -S sethostname -S setdomainname -k network_modifications
{% endfor %}
