# Managed by Ansible. Local changes will be overwritten.

# Privileged processes with setuid or setgid bits
{% for audit_privileged_command in audit_priv_command %}
-a always,exit -F path={{ audit_privileged_command }} -F perm=x -F auid>={{ audit_uid_min }} -F auid!=unset -k privileged
{% endfor %}
